export enum SortDirection {
  asc = 'asc',
  desc = 'desc',
}

export enum ImageableEntity {
  User = 'User',
}

// Unique identifier of the service in the dependency injection layer
export const S3Lib = 'lib:s3';
